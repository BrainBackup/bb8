version: '3.6'

services:
  mysql:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: w3lc0me!
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080    

